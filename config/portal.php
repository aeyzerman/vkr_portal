<?php

return [

    /*
    |--------------------------------------------------------------------------
    | Учётная запись администратора по умолчанию
    |--------------------------------------------------------------------------
    |
    | Задайте ADMIN_EMAIL и ADMIN_PASSWORD — пользователь будет создан или
    | обновлён командой `php artisan portal:ensure-admin` и при db:seed.
    |
    */

    'admin' => [
        'name' => env('ADMIN_NAME', 'Administrator'),
        'email' => env('ADMIN_EMAIL'),
        'password' => env('ADMIN_PASSWORD'),

        /*
         * Секрет для поля на странице регистрации. Если в системе ещё нет
         * администратора и токен совпал — новому пользователю выдаётся PERM_ADMIN.
         * Оставьте пустым, чтобы отключить.
         */
        'registration_token' => env('ADMIN_REGISTRATION_TOKEN'),
    ],

];
